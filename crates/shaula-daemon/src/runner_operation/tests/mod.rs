//! Runner Operation matrix: every scenario runs through the same Interface for
//! both Runner Backend adapters, proving the backend never changes the rules.

mod fakes;
mod fixture;
mod lifetime;
mod retire;
mod seed;

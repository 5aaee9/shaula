# PAT / CLI deployment evidence — 2026-09-25

## Deployment identity

- Entry: `ssh -p 2222 indexyz@pandar.nix.ac.cn`, verified host `plasma`.
- PowerArmor: `/home/indexyz/Projects/PowerArmor`, baseline commit
  `5e768e9fef8f6f987c4a4fe7414c5e7ed094e6fc`.
- Deployment used a working-tree snapshot based on merged PR #3
  (`d112de6c6ce42f36984838f597a40744fa6dd4aa`), before committing the implementation.
- Snapshot: `/home/indexyz/Projects/shaula-pat-20260925`; locked NAR hash
  `sha256-io4LwQeERZZ3UJ07QSiXEl1wVcsSEOKkz4M1BEFn1t8=`.
- PowerArmor `flake.lock` points to that snapshot. The existing Indexyz operator
  grant additionally has `access-token.read`, `access-token.write`, and
  `access-token.revoke`. These two deployment files remain uncommitted.
- An isolated PowerArmor worktree at `.worktrees/shaula-pat-20260925` was used
  for preparation; the successful configuration was copied to the main checkout.
  The adjacent remote Shaula checkout's unrelated edits were preserved.

## Build and activation

`nix develop -c colmena build --on molecule --build-on-target` passed, including
release compilation, strict all-target Clippy, **949 nextest passes / 2 skipped**,
and **2 Terraform HTTP-state tests**. Windows previously passed 955 nextest tests;
platform-specific tests explain the separate counts.

The deployment exposed CRLF shell fragments in copied Nix files; the snapshot
was normalized and `.gitattributes` now requires LF for Nix. The Nix source filter
also includes the route inventory consumed by compile-time acceptance fixtures.

`nix develop -c colmena apply --on molecule --build-on-target` completed with
`Activation successful`. The user explicitly included the complete molecule
system update.

- Active system: `/nix/store/86p2gp2mp31p56y3wspfbl91mbpawfw3-nixos-system-molecule-26.11pre-git`.
- Running binary: `/nix/store/afr3mrnv9xl2gwig845q4syg2dd0fbm9-shaula-0.1.0/bin/shaula`.
- Build log on plasma: `/home/indexyz/Projects/shaula-pat-build-final.log`.
- Activation log on plasma: `/home/indexyz/Projects/shaula-pat-activation.log`.

## Data and service checks

Root-only backups on molecule are under `/var/backups/shaula/pat-20260925`.
`shaula-online.db` is a SQLite online backup; `state/` is a complete Btrfs reflink
copy taken after stopping Shaula. The original system and binary paths are also
recorded there. Both backup and live `PRAGMA quick_check` returned `ok`.

Migrations advanced from m0019 through m0025. Shaula is `active/running`, with
zero automatic restarts. No failed systemd units or warning-priority Shaula
journal entries appeared in the deployment interval. All five scale-set rows
remain `Adopted`. Runner state counts match the pre-deploy baseline: 299 + 67 +
173 Destroyed, and three pre-existing Quarantined rows.

The actual HTTPS Browser session recovered the Indexyz operator identity and
displayed the new Access tokens page. Both current Fleets were `Ready`:
`animittahq-pve` at revision 4 and `droidterm-pve` at revision 2.

## Actual OIDC / PAT acceptance

After the user's confirmation, the browser issued one temporary token named
`Deployment acceptance 2026-09-25`, with only `fleet.read` and a one-day TTL.
Its secret was supplied through hidden terminal input and CLI stdin, retained
only in the acceptance process's memory, and never saved in a credential file.

- Real Windows CLI over `https://shaula.indexyz.me`: `auth whoami` identified
  `personal_access_token`, the expected owner and exactly `fleet.read`.
- `fleets list` returned the two current Fleets; `tokens current` and
  `health ready` succeeded. `auth-profiles list` was denied with HTTP 403 / exit 4.
- The browser's explicit verification reported `New token saved and verified`
  and removed the revealed secret from the page.
- `systemctl restart shaula` changed PID 1484633 to 1484862. The same PAT then
  passed `auth whoami`, `fleets list`, and `tokens current` again.
- `tokens revoke --current --yes` succeeded. Reusing the credential for
  `auth whoami` returned HTTP 401 / exit 3.
- SQLite confirmed persistent revocation and zero unrevoked PAT rows. Browser
  reauthentication displayed the token as `revoked`, without a secret.
- Post-test database integrity was `ok`; Shaula remained active/running with
  zero automatic restarts and no warning-priority journal entries.

No valid acceptance credential remains. This proves browser-primary issuance,
actual CLI authorization, a denied scope, restart persistence and revocation
against the deployed Provider and HTTPS origin.

## Remaining acceptance boundaries

No production Provider outage was induced and no live backup restore was
performed. Kanidm's existing client registration cannot mint the distinct API
audience needed for primary OIDC JWT CLI issuance; browser primary-OIDC issuance
is the available deployment path. Full WF-01–WF-14 paired failure-branch parity
is still tracked in [implementation status](../IMPLEMENTATION_STATUS.md).

Keep the locked source snapshot until it is replaced by a published immutable
revision. A binary-only rollback is unsafe across the idempotency migration;
restore the matching pre-upgrade state with the old binary.

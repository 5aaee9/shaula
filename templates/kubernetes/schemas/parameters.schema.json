{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Shaula Kubernetes Profile Fleet inputs",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "runner_image": {
      "type": "string",
      "enum": [
        "ghcr.io/actions/actions-runner:2.337.0"
      ],
      "description": "Finite official GitHub Runner image alias; profile.yaml pins the unmodified upstream image digest."
    },
    "cpu_request": {
      "type": "string",
      "enum": ["500m", "1", "2"]
    },
    "memory_request": {
      "type": "string",
      "enum": ["2Gi", "4Gi", "8Gi"]
    }
  }
}
